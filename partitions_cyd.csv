# CYD (ESP32-2432S028R) has 4MB flash, unlike the Waveshare S3 board's 16MB (partitions_16M.csv).
# Single factory app, no OTA slot -- but DOES need its own "storage" SPIFFS partition (same
# partition_label="storage" src/util/storage_mount.cpp mounts on the S3): hfs_radial.cpp/
# orbital_library.cpp load hfs_tables.bin/orbital_samplers.bin from it on demand, falling back
# to a degraded model (hydrogenic atoms / single-point orbitals) if the mount or the files are
# missing -- not a boot failure, but a visibly broken default view on this board's tilt-less
# auto-launch (see CYD-branch.md). mkspiffs needs headroom well above the raw data/ payload size
# for its page/block metadata (~15% at page=256/block=4096 -- 1,179,648B nominally free failed
# to fit 923,419B of data/ payload, "SPIFFS_write error(-10001): File system is full"); 1.5MB
# leaves comfortable slack over the ~902KiB payload (atomic_cube.jpg + hfs_tables.bin +
# orbital_samplers.bin) for both that overhead and future growth. factory shrunk to match --
# firmware.bin is ~275KB, far under even the reduced size. Deploy the fs image with
# `pio run -e CYD -t uploadfs` (see platformio.ini's extra_scripts comment).
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x6000,
phy_init,   data, phy,      0xf000,   0x1000,
factory,    app,  factory,  0x10000,  0x270000,
storage,    data, spiffs,   0x280000, 0x180000,
