// All of orbitals.h's functions are now header-only constexpr (see the
// comment at the top of orbitals.h for why). Nothing left to define here --
// this file only exists so build scripts/globs that list it as a translation
// unit (tools/orbitals_host/run_crosscheck.sh, src/CMakeLists.txt's glob)
// keep working unchanged.
#include "physics/orbitals.h"
